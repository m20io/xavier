%h1 Define a new team
%p Please enter the team name.

- if @team.errors.any?
  #error_explanation
  %h2= pluralize(@team.errors.count, 'error') +  ' prohibited this post from being saved:'
  %ul
    - @team.errors.full_messages.each do |message|
      %li= message

= form_for @team do |f|
  = f.label :name
  = f.text_field :name
  = f.submit 'create team'